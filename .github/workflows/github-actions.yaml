name: GitHub Actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ls -l
      - run: docker-compose build
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: docker-compose up -d